# apps/mobile – Setup

This folder will contain the Expo React Native learner app.

## Initialize locally

Run this command from the repo root:

```bash
npx create-expo-app@latest apps/mobile
```

Then start the app:

```bash
cd apps/mobile
npm run start
```

## Status
- [ ] Expo app initialized locally
- [ ] First screen: Subject list
